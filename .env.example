# 服务器地址与端口
HOST=127.0.0.1
PORT=25565

# 假人用户名（离线模式）
BOT_USERNAME=mcbot

# 服务器版本，必须与服务器完全一致（协议不同会连不上）
VERSION=1.21.11

# 登录方式：offline（离线服 + AuthMe）/ microsoft（正版账号）
AUTH=offline

# AuthMe /login 密码（必填，启动时缺少会直接退出）
PSW=change_me

# 网页监控端口（监控台页面）与 3D 视图端口
WEB_PORT=3000
VIEWER_PORT=3001
